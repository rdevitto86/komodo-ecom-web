/* eslint-disable no-shadow */
/* eslint-disable no-unused-vars */

/**
 * Registered API domains used across applications
 */
export enum Domains {
    Security,
    User,
    Catalog,
    Order,
    Finance,
    HR,
    Logging,
    Analytics,
}
