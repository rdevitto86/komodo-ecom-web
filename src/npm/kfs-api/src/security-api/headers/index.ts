import { RequestHeaders } from '../../common/headers/request';

/**
 * Headers for user login requests
 * @extends RequestHeaders
 */
export class LoginHeaders extends RequestHeaders {

}

/**
 * Headers for user logout requests
 * @extends RequestHeaders
 */
export class LogoutHeaders extends RequestHeaders {

}

/**
 * Headers for validate session requests
 * @extends RequestHeaders
 */
export class ValidateSessionHeaders extends RequestHeaders {

}
