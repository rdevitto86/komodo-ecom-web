// import PaymentMethod from '../payment-methods/payment-method.model';
// import Billing from './billing.model';
export {}; // TODO - remove

describe('Billing Model tests', () => {
    // const fullMock = new Billing({
    //     defaultMethod: new PaymentMethod(),
    //     paymentMethods: [],
    // });

    // const partialMock = new Billing({
    //     defaultMethod: null,
    //     paymentMethods: [],
    // });

    // TODO - stub/spies

    test('model w/ params fully instatiates model', () => {
        // const {
        //     defaultMethod,
        //     paymentMethods,
        // } = fullMock;

        // expect(defaultMethod).toBe({});
        // expect(paymentMethods).toBe([]);
    });

    test('model w/ params partially instatiates model', () => {
        // const {
        //     defaultMethod,
        //     paymentMethods,
        // } = partialMock;

        // expect(defaultMethod).toBe(null);
        // expect(paymentMethods).toBe([]);
    });

    test('model w/ no params', () => {
        // const {
        //     defaultMethod,
        //     paymentMethods,
        // } = new Billing();

        // expect(defaultMethod).toBe(null);
        // expect(paymentMethods).toBe([]);
    });

    test('model w/ another billing model', () => {
        // const {
        //     defaultMethod,
        //     paymentMethods,
        // } = new Billing(fullMock);

        // expect(defaultMethod).toBe({});
        // expect(paymentMethods).toBe([]);
    });

    test('getPaymentMethod returns correct data', () => {

    });

    test('addPaymentMethod inserts correct data', () => {

    });

    test('removePaymentMethod removes correct data', () => {

    });

    test('setDefaultPayment sets correct data', () => {

    });

    test('clearDefaultPayment removes correct data', () => {

    });

    test('clearPaymentMethods removes all data', () => {

    });
});
